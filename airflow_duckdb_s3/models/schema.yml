version: 2

models:
  - name: stichtag_report
    description: Aggregation for one mandant and the DAG-selected reporting dates.
    columns:
      - name: mandant
        tests: [not_null]
      - name: stichtag
        tests: [not_null]

