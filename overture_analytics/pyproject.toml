[project]
name = "overture-analytics"
version = "0.1.0"
description = "Configurable dbt-duckdb analytics over anonymous Overture Maps S3 GeoParquet"
requires-python = ">=3.10"
dependencies = [
  "dbt-duckdb>=1.9,<2",
  "duckdb>=1.4,<2",
]

[tool.setuptools]
packages = []
