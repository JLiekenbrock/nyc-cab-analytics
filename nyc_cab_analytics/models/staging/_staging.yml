version: 2

models:
  - name: stg_yellow_trips
    description: Cleaned and typed yellow taxi trips.
    columns:
      - name: pickup_at
        data_tests: [not_null]
      - name: dropoff_at
        data_tests: [not_null]
  - name: stg_taxi_zones
    columns:
      - name: location_id
        data_tests: [not_null, unique]
